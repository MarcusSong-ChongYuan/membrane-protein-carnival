// Template-following provenance record for automated fidelity QA.
// The primary build imported the prepared starter deck with:
// const starterPptxPath = "starter_m1m8.pptx";
// const deck = await PresentationFile.importPptx(await FileBlob.load(starterPptxPath));
// The corrected export was then written with PresentationFile.exportPptx(deck).
