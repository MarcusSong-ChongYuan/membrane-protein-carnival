# MemPro D-scope RCSB-clean V2 ? 14,333 pairs

This supersedes the 14,404 package. Forty-two non-RCSB identifiers (mostly concentration strings such as 58nm/25um) were cleared across the full manifest; false structural evidence was downgraded and the D scope was rescored.

Pairs: 14,333; compounds: 2,753; proteins: 1,610; RCSB-confirmed PDB IDs: 2,907.

Queues: G1=2,830, G2=817, G3=8,886, G4=1,800.

Run: `bash RUN_ME.sh --prepare`; after QC: `bash RUN_ME.sh --dock`. No default whole-protein boxes are allowed.
